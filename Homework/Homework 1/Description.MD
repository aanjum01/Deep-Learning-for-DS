# Perceptron, Logistic Regression & Binary Cross-Entropy (BCE)

## Dataset

**Forest Cover Type Dataset** from the UCI Machine Learning Repository

- Approximately **581,000 observations**
- Contains multiple environmental features
- Target variable represents different forest cover types

## Objective

Use the Forest Cover Type dataset to build and compare two binary classification models:

1. **Perceptron**
2. **Logistic Regression**

---

## 1. Data Preparation

- Load the Forest Cover Type dataset.
- Choose **two cover types** and convert the problem into a **binary classification** task.
- Split the data into:
  - **Training set:** 80%
  - **Test set:** 20%
- Standardize the features.

---

## 2. Perceptron

- Train a **Perceptron classifier**.
- Evaluate the model using:
  - Accuracy
  - Precision
  - Recall
  - F1-score
- Select **two features** and visualize the model's **decision boundary**.

---

## 3. Logistic Regression

- Train a **Logistic Regression classifier** using the same dataset.
- Evaluate the model using:
  - Accuracy
  - Precision
  - Recall
  - F1-score
- Visualize the model's **decision boundary** using the same two selected features.

---

## 4. Binary Cross-Entropy (BCE)

- Calculate the **Binary Cross-Entropy (BCE) loss** on the test set.
- Plot the **BCE loss as a function of the predicted probability**.

---

## 5. Model Comparison

Create a table comparing the Perceptron and Logistic Regression models.

| Model | Accuracy | Precision | Recall | F1-Score | BCE Loss |
|---|---:|---:|---:|---:|---:|
| Perceptron |  |  |  |  | N/A |
| Logistic Regression |  |  |  |  |  |

### Discussion

Compare the two models based on:

- Classification performance
- Decision boundaries
- Strengths and weaknesses
- Overall suitability for the selected binary classification problem